angular.module('snowy.rateservice', []).
  // Declare new object called time,
  // which will be available for injection
	factory('rate', function(remotedata, localdata) {	
		var	currresort = null;
		return {			
		};
	});